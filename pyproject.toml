[project]
name = "brainfoo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=8.2.8",
]
[dependency-groups]
dev = [
    "nuitka>=4.1.3",
    "pylint",
    "pytest",
]
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

